{
  "id": "window",
  "label": "RouteDEM",
  "targetScript": "natcap.invest.routing.routedem",
  "modelName": "routedem",
  "localDocURI": "../documentation/routedem.html",
  "height": 370,
  "width": 500,
  "elements": [
    {
      "id": "routedem_list",
      "type": "list",
      "elements": [
        {
          "id": "workspace_dir",
          "args_id": "workspace_dir",
          "type": "folder",
          "label": "Workspace",
          "defaultValue": "~/Documents/routedem_workspace",
          "required": true,
          "helpText": "The selected folder is used as the workspace where all intermediate and output files will be written.<br><br>If the selected folder does not exist, it will be created.<br><br><b>If datasets already exist in the selected folder, they will be overwritten.</b>"
        },
        {
          "id": "dem_path",
          "args_id": "dem_path",
          "type": "file",
          "label": "Digital Elevation Model (Raster)",
          "validateAs": {
            "type": "GDAL"
          },
          "required": true,
          "defaultValue": "../Base_Data/Freshwater/dem",
          "helpText": "A GDAL-supported raster file containing a base Digital Elevation Model to execute the routing functionality across."
        },
        {
          "id": "calculate_stream_threshold",
          "args_id": "calculate_stream_threshold",
          "type": "checkbox",
          "label": "Calculate Stream Thresholds",
          "defaultValue": false,
          "required": false,
          "helpText": "Select to calculate a stream threshold to flow accumulation."
        },
        {
          "id": "threshold_flow_accumulation",
          "args_id": "threshold_flow_accumulation",
          "type": "text",
          "label": "Threshold Flow Accumulation Limit",
          "defaultValue": "1000",
          "required": true,
          "validText": "^[0-9]+$",
          "enabledBy": "calculate_stream_threshold",
          "helpText": "The number of upstream cells that must flow into a cell before it's classified as a stream."
        },
        {
          "id": "calculate_downstream_distance",
          "args_id": "calculate_downstream_distance",
          "type": "checkbox",
          "label": "Calculate Distance to stream",
          "defaultValue": false,
          "required": false,
          "enabledBy": "calculate_stream_threshold",
          "helpText": "If selected, creates a downstream distance raster based on the thresholded flow accumulation stream classification."
        },
        {
          "id": "calculate_slope",
          "args_id": "calculate_slope",
          "type": "checkbox",
          "label": "Calculate Slope",
          "defaultValue": false,
          "required": false,
          "helpText": "If selected, calculates slope raster."
        }
      ]
    }
  ]
}
